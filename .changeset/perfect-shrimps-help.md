---
'@inventage-web-components/portal-navigation': minor
---

Adds a `fallbackLanguage` property to use as fallback (key) in label translations. When this exists as a key in the label object, the value of that key will be used for the label instead of falling back to an empty string.
